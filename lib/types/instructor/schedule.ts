// **** Schedule *****
export type ScheduleType = {
  subject: string;
  date: string;
  session: string;
  class: string;
  id: string;
  status: string;
};

export type StatusOption = {
  label: string;
  value: string;
};
